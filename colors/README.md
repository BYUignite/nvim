These two colorschemes are local, but match those in the forsake-repent plugin on github.

These can be used with  
`:colorscheme repent2` or  
`:colorscheme forsake2`,   
or in your lua configuration, use  
`vim.cmd("colorscheme repent2")` or  
`vim.cmd("colorscheme repent2")`.
